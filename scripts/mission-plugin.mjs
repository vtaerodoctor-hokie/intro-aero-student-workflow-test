import { collect } from './mission-state.mjs';
export function missionPlugin(){let root;return {name:'weekly-mission-state',configResolved(c){root=c.root;},resolveId(id){if(id==='virtual:mission-state')return '\0virtual:mission-state';},load(id){if(id==='\0virtual:mission-state')return `export default ${JSON.stringify(collect(root))};`;},configureServer(server){server.watcher.add([`${root}/student-work/missions`,`${root}/.instructor/private/mission-verification.json`]);},handleHotUpdate({file,server}){if(file.includes('/student-work/'))return [];if(!file.includes('/node_modules/')&&!file.includes('/dist/')){const mod=server.moduleGraph.getModuleById('\0virtual:mission-state');if(mod)server.moduleGraph.invalidateModule(mod);server.ws.send({type:'full-reload'});}}};}
